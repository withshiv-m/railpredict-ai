# Copy this file to .env and fill in real values.
# If WEATHER_API_KEY is left blank, the app automatically falls back to
# simulated/demo weather data, so the project runs fully offline.
WEATHER_API_KEY=
DATABASE_URL=sqlite:///./database.db
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
